// Test-only project; doesn't use expo-module-gradle-plugin (would need host app). Module Kotlin sources compile directly without Expo.
pluginManagement {
  repositories {
    google()
    mavenCentral()
    gradlePluginPortal()
  }
}

dependencyResolutionManagement {
  repositories {
    google()
    mavenCentral()
  }
}

rootProject.name = 'expo-gatt-server-android-tests'
